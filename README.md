# azure-banco-de-dados-mysql

# Instalar DBeaver


<https://dbeaver.io/download/>


# Instalar Cliente MySQL

```
$ telnet <url do MySQL no Azure> 3306 
$ apt-cache search mysql-client
$ sudo su -
$ apt update && apt install mysql-client -y
```

